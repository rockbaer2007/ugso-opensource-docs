# Entities

Entities are created through MQTT Discovery.

For each enabled source and configured amount, the app creates:

- price per 100 liters
- total price
- dealer
- delivery days
- offer count

It also creates:

- Heizöl connection
- Heizöl last update

Detail attributes contain source, amount, postal code, dealer, delivery date, rating and offer count.
